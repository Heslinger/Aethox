# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Aethox") is currently an empty scaffold with only a `README.md` containing git setup instructions. There is no application code, no dependency manifests, no build system, and no services to run.

- **No dependencies to install** — no `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, or similar files exist.
- **No lint/test/build/run commands** — nothing to execute.
- **No services** — no backend, frontend, database, or other infrastructure.

When code is added to this repository, update this file with relevant development instructions (how to install, lint, test, build, and run).
